=========================
Output visualizations
=========================


------------------------------------
Matrix column visualizations
------------------------------------

Many Biotaphy matrices encode sites (cells representing a gridcell in an area of
interest).  Any column in these

------------------
Output Statistics
------------------

Individual statistics from a site-statistics matrix (created with calculate_pam_stats)
may be mapped as a heatmap, and plotted as ...


create_stat_heatmap

A file path to write the generated plot image.  Available formats are: " +
        "eps, pdf, pgf, png, ps, raw, rgba, svg, svgz

